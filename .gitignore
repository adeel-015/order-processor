# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr
.project
.classpath
.settings/
.env

# Maven
target/
pom.xml.tag
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
build/
*.jar
*.war
*.ear
*.zip
*.tar.gz
*.log
logs/

# Java
*.class

# Log files
*.log
*.log.*
.logs/

# Environment variables
.env
.env.local
.env.*.local
*.properties.bak

# Docker and Kubernetes related files that are often local/generated
# (Ensure these are not your actual K8s manifests you want to commit)
# If 'docker' and 'grafana' etc. are just local volume mounts, ignore them.
# If they contain configuration files you want to commit, then be specific.
docker/
grafana/
mongo-data/
mysql/
mysql_keycloak_data/
postgres-inventory/
postgres-order/

# Specific files generated by Docker Compose or other tools that are temporary
docker-compose.override.yml
.DS_Store # macOS specific hidden folder settings
*.swp # Vim swap files
*.bak # Backup files
*~ # Emacs backup files
# For JIB specific build artifacts (usually handled by Maven target/)
# If JIB creates any non-target files, you can add them here.
# For example, if you explicitly tell JIB to output to a specific dir outside of target/